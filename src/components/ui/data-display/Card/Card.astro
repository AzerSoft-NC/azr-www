---
import type { HTMLAttributes } from 'astro/types';
import { cn } from '@/lib/cn';
import { cardVariants } from './card.variants';

interface Props extends HTMLAttributes<'div'>, Pick<HTMLAttributes<'a'>, 'target' | 'rel'> {
  variant?: 'default' | 'solid' | 'outline' | 'ghost' | 'elevated';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  hover?: boolean;
  /** 1:1 aspect ratio (width from layout; height follows). */
  square?: boolean;
  /** Flex + text alignment to center slot content (pairs well with `square`). */
  centerContent?: boolean;
  href?: string;
}

const {
  variant = 'default',
  padding = 'md',
  hover = false,
  square = false,
  centerContent = false,
  href,
  class: className,
  ...attrs
} = Astro.props;

const Element = href ? 'a' : 'div';

const cardStyles = cn(
  cardVariants({ variant, padding, hover }),
  href && 'block cursor-pointer',
  square && 'aspect-square',
  centerContent && 'flex min-h-0 flex-col items-center justify-center text-center',
  className
);
---

<Element class={cardStyles} href={href} {...attrs}>
  <slot />
</Element>
