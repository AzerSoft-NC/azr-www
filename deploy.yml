stack: astro
docker:
  compose: 'docker-compose.yml'
commands:
  format: ['pnpm format']
  check: ['pnpm lint', 'pnpm astro:check']
  test: ['pnpm test']
local:
  env: '.env.local'
prod:
  droplet: ubuntu-azr-1
  env: '.env.prod'
